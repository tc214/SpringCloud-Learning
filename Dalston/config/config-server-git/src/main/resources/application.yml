spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tc214/config-repo-demo/
#          search-paths: repo
         # username: 
        # password: 

server:
  port: 1201